import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'purchase-Order-Ferroca',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
